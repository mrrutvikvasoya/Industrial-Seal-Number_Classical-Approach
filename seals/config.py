"""Constants shared across the pipeline."""

# Every seal code is exactly seven digits.
DIGIT_COUNT = 7
# Largest row tilt the localizer accepts, in degrees.
MAX_ANGLE = 15.0
# Fixed seed so sampling and augmentation are reproducible.
DEFAULT_SEED = 42
